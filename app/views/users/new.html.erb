<h1>Create new user</h1>
<%= form_for @user, :html => {:class => 'form-horizontal'} do |f| %>
  <div class='form-group'>
    <%= f.text_field :name, placeholder: 'Name' %>
  </div>
  <div class='form-group'>
    <%= f.submit 'New user', class: 'btn btn-default' %>
  </div>
<% end %>